server {
    listen 80;
    server_name localhost;

    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        
        # THE FIX: Try to serve the exact file, then the folder. 
        # If neither exists, fall back and serve index.html
        try_files $uri $uri/ /index.html;
    }
}